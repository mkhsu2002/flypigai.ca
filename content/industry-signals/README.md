# Industry Signals content

Each JSON record is editorially rewritten from an official supplier source and is rendered through the shared `/signals/[slug]` template. Hero graphics marked `original_infographic` are generated by `scripts/generate-signal-heroes.mjs` from the record metadata before development and production builds. Project-specific supplier claims, commercial terms and availability must remain explicitly qualified unless verified from the official source.
